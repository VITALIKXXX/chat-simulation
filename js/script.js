const inputBox = document.querySelector(".chat__input");
const chatBox = document.querySelector(".chat__box");
const sendButton = document.querySelector(".chat__button");

const sendMessage = () => {
    const messag = inputBox.value.trim();
    if (!messag) return;


};